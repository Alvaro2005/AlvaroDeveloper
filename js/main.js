const type = new Typed('.typed', {
    strings: ['Hello, I m Alvaro patiño,front end developer, take a look at my portfolio'],
    // stringsElement: '#cadenas-texto', // ID del elemento que contiene cadenas de texto a mostrar.
    typeSpeed: 50,
    startDelay: 50,
    backSpeed: 20,
});